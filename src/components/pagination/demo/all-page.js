import React from 'react'
import Pagination from 'tb-pagination'

export default class Demo extends React.Component {
    render() {
        return (
            <Pagination allPage={6}/>
        )
    }
}